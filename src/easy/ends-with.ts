//=========================================Instructions
//Complete the solution so that it returns true if the...
//first argument(string) passed ends with the 2nd argument(also a string).
function endsWith(str: string, ending: string): boolean {
  return str.endsWith(ending);
}
